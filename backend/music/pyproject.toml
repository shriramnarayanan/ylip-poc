[project]
name = "ylip-music-backend"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
    "scipy>=1.10",
    "numpy>=1.24",
    "fastapi>=0.100",
    "uvicorn>=0.24",
]

[tool.uv]
package = false
