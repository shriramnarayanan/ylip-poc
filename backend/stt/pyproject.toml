[project]
name = "ylip-stt-backend"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "faster-whisper>=1.0",
    "fastapi>=0.100",
    "uvicorn>=0.24",
    "python-multipart>=0.0.9",
]

[tool.uv]
package = false
